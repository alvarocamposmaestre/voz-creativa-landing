# Checklist: outcomes_mobile_layout

- [x] Add the "Conocer más" button to Raquel Cartaya's bio in `index.html` pointing to `https://raquelcartayastudio.com/`.
- [x] Add base `.btn-conocer-mas` styles in `style.css` (display: none, margin-top).
- [x] Configure `@media (max-width: 992px)` outcomes section mobile ordering in `style.css` using `display: flex; flex-direction: column;` and `order`.
- [x] Show `.btn-conocer-mas` in the mobile viewport using `@media (max-width: 992px)` in `style.css`.
- [x] Center the branded logo image (`.signup-logo-wrapper`) inside its container in the final section in `style.css`.
